// Static counts shown in OG images, marketing copy, etc.
// Updating these is a deliberate edit — the live numbers live in `data.ts`
// (which is too heavy to import from edge/serverless OG routes).

export const TOTAL_ITEMS = 5488;
export const TOTAL_RECIPES = 2211;
